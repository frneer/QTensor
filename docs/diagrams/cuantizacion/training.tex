\begin{tikzpicture}[
    block/.style={rectangle, draw, fill=white, text width=4em, text centered, rounded corners, minimum height=2em},
    rectangular_block/.style={rectangle, draw, fill=white, text width=4em, text centered, minimum height=2em},
    arrow/.style={-latex, thick}
]
    \tikzset{node distance = 1cm and 2cm}
    % Nodes
    \node (x_input) at (0, 0) [rectangular_block] {$x$};
    \node (w_input) [below=of x_input] [rectangular_block] {$\theta$};
    \node (forward_pass) [right=of w_input] [block] {\textit{forward pass}};
    \node (loss) [below=of forward_pass] [block] {$\mathcal{L}$};
    \node (y_input) [below=of loss] [rectangular_block] {$y$};
    \node (gradient) [left=of loss] [block] {\textit{backward pass}};

    % % Arrows
    \draw [arrow] (x_input) -| (forward_pass);
    \draw [arrow] (w_input) -- (forward_pass); % Connect W to forward pass
    \draw [arrow] (y_input) -- (loss);
    \draw [arrow] (forward_pass) -- (loss);
    \draw [arrow] (loss) -- (gradient);
    \draw [arrow] (gradient) -- (w_input); % Gradient updates W

% This version creates a dotted outline instead of a fill
\begin{pgfonlayer}{background}
    \node [
        draw,                   % <-- Add this to draw the border
        dotted,                 % <-- Add this for the dotted style
        thick,                  % (Optional) Make the dots thicker
        blue,                   % (Optional) Color for the dots
        rounded corners,
        fit=(w_input) (forward_pass) (loss) (gradient),
        inner sep=0.5cm
    ] {};
\end{pgfonlayer}

\end{tikzpicture}
